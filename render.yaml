services:
  - type: web
    name: washi-rides
    env: node
    nodeVersion: "18"          # ✅ Forces Node 18
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
